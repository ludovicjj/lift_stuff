{{ form_start(formLift, {
    'attr': {
        'novalidate': 'novalidate',
        'class': 'js-new-rep-log-form',
        'action': path('rep_log_add')
    }
}) }}
    {{ form_errors(formLift) }}

    <div class="row">
        {{ form_row(formLift.item, {
            'row_attr': {
                'class': 'col',
            }
        }) }}
        {{ form_row(formLift.reps, {
            'row_attr': {
                'class': 'col',
            },
            'attr': {'placeholder': 'How many times?'}
        }) }}
        <div class="col">
            <button type="submit" class="btn btn-blue">I Lifted it!</button>
        </div>
    </div>
{{ form_end(formLift) }}